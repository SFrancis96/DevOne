<% if user_signed_in? %>
<section id="welcome-section" class="min-h-screen grid place-items-center">
    <div class="w-5/6 h-fit">
        <%# <%= debug current_user %>
        <div class="flex flex-col sm:flex-row justify-between sm:items-center">
            <div class="mb-10">
                <h1 class="font-bold text-2xl sm:text-2xl md:text-4xl mb-5">Bienvenue<br> <span class="text-6xl sm:text-8xl lg:text-9xl"><%= current_user.username%>!  &#x1F600;</span></h1>
                <p class="text-xl">Télécharge le programme<br>et commence à miner tes cryptos <span class="font-semibold italic">DevOne</span> !</p>
            </div>
            <div>
                <p class="text-xl sm:text-4xl mb-4 sm:mb-6">Télécharger le programme</p>
                <a href="#" class="btn-primary text-xl w-full mx-auto text-center">Télécharger</a>
            </div>
        </div>
    </div>
</section>
<% else %>
    <% controller.redirect_to root_path %>
<% end %>
